#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>
#include "main.h"

/**
 * _atoi - Return a segmented string using pointers
 * @s: Initial segment pointer
 *
 * Return: Number of bytes occurring in another string
 */

int _atoi(char *s)
{
char *a;
a = s;

return (0);
}
